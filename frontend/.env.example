# Origin of the API; omit this value in a same-origin deployment.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
# Internal backend origin used by the server-side same-origin API proxy.
API_PROXY_TARGET=http://backend:4000
